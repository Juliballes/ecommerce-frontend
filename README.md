El proyecto está organizado en cuatro carpetas. pages/ tiene las páginas que se renderizan según la ruta (Home, ProductDetail, Carrito, Login, Register, AgregarProducto). components/ tiene los componentes reutilizables que se usan dentro de las páginas (Navbar, ProductList, ProductListCategoria, CardProductos). context/ tiene el estado global (AuthContext, CarritoContext).
